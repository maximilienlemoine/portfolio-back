{% extends 'base.html.twig' %}

{% block title %}Objectifs{% endblock %}

{% block body %}
    <h1>Objectifs</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ goal.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ goal.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_goal_index') }}">Retour à la liste des objectifs</a>

    <a href="{{ path('app_goal_edit', {'id': goal.id}) }}">Modifier</a>

    {{ include('goal/_delete_form.html.twig') }}
{% endblock %}
