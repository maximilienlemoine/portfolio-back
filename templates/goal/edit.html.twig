{% extends 'base.html.twig' %}

{% block title %}Modifier Objectif{% endblock %}

{% block body %}
    <div class="d-flex justify-content-between align-items-center">
        <h1>Modifier l'objectif "{{ goal.id }} - {{ goal.title }}"</h1>
        <div class="d-flex gap-3">
            {{ include('goal/_delete_form.html.twig') }}
            <a class="btn btn-secondary" href="{{ path('app_goal_index') }}">Retour à la liste des objectifs</a>
        </div>
    </div>
    {{ include('goal/_form.html.twig', {'button_label': 'Modifier'}) }}
{% endblock %}
