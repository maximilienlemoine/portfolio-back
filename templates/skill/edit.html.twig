{% extends 'base.html.twig' %}

{% block title %}Modifier compétence{% endblock %}

{% block body %}
    <div class="d-flex justify-content-between align-items-center">
        <h1>Modifier la compétence "{{ skill.id }} - {{ skill.title }}"</h1>
        <div class="d-flex gap-3">
            {{ include('skill/_delete_form.html.twig') }}
            <a class="btn btn-secondary" href="{{ path('app_skill_index') }}">Retour à la liste des compétences</a>
        </div>
    </div>

    {{ include('skill/_form.html.twig', {'button_label': 'Modifier'}) }}
{% endblock %}
