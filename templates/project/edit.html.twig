{% extends 'base.html.twig' %}

{% block title %}Modifier Project{% endblock %}

{% block body %}
    <div class="d-flex justify-content-between align-items-center">
        <h1>Modifier le projet "{{ project.id }} - {{ project.title }}</h1>
        <div class="d-flex gap-3">
            {{ include('project/_delete_form.html.twig') }}
            <a class="btn btn-secondary" href="{{ path('app_project_index') }}">Retour à la liste des projets</a>
        </div>
    </div>

    {{ include('project/_form.html.twig', {'button_label': 'Modifier'}) }}
{% endblock %}
